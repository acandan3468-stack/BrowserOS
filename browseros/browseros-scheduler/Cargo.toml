[package]
name = "browseros-scheduler"
version = "0.1.0"
edition = "2021"
description = "Minimal delayed execution and event-triggered scheduling for BrowserOS"

[dependencies]
browseros-types = { path = "../browseros-types" }
browseros-event-bus = { path = "../browseros-event-bus" }
chrono = { version = "0.4", features = ["serde"] }
